# 爬取流程设计

## 概述

本文档描述 CJ-Spider 的爬取流程设计，基于安徽财经网会员发布页面进行新闻文章爬取。

---

## 1. 页面结构分析

### 1.1 列表页面

**URL**: `https://www.ahcaijing.com/index.php?m=member&c=content&a=published&page={page}`

**页面编码**: GBK

**HTML 结构**:
```html
<table>
  <tr>
    <td align="center">{文章ID}</td>
    <td align="left"><a href="{文章链接}">{标题}</a></td>
    <td align="center">{分类}</td>
    <td align="center">{发布时间}</td>
    <td align="center">{状态}</td>  <!-- 只有"通过"状态的文章才可爬取 -->
  </tr>
</table>

<!-- 分页信息 -->
<a class="a1">{总条数}条</a>
```

**提取字段**:
| 字段 | 说明 | 提取方式 |
|------|------|----------|
| id | 文章ID | `td[align="center"]:first` |
| link | 文章链接 | `td[align="left"] a[href]` |
| topic | 分类 | `td[align="center"]:eq(1)` |
| createdTime | 发布时间 | `td[align="center"]:eq(2)` |
| status | 状态 | `td[align="center"]:eq(3)` |
| total | 总条数 | `a.a1` 文本提取数字 |

### 1.2 详情页面

**URL**: 从列表页获取的文章链接

**页面编码**: GBK

**HTML 结构**:
```html
<div class="crumbs">
  <a>首页</a> > <a>地市</a> > <a>{地区}</a>
</div>
<div class="article-hd">
  <h1>{标题}</h1>
</div>
<span class="time">{发布时间}</span>
<div class="source">来源：{来源} 编辑：{编辑}</div>
<div id="text_content">{正文HTML}</div>
```

**提取字段**:
| 字段 | 说明 | 提取方式 |
|------|------|----------|
| title | 标题 | `.article-hd h1` |
| content | 正文HTML | `#text_content` |
| source | 来源 | `.source` 文本分割 |
| author | 作者 | 正文末尾括号内容 |
| topic | 分类 | `.crumbs a:last` |
| createdTime | 发布时间 | `span.time` |
| area | 地区 | 从标题分析 |

---

## 2. 爬取流程

### 2.1 流程图

```
┌─────────────────────────────────────────────────────────────┐
│                     用户创建爬取任务                          │
│              (指定页码范围: startPage ~ endPage)              │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                   1. 验证登录状态                             │
│              检查 Cookie 是否有效                             │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                   2. 创建爬取会话                             │
│         记录任务配置 (页码范围、创建时间等)                    │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                   3. 逐页爬取列表                             │
│                                                             │
│   for page in range(startPage, endPage + 1):                │
│     ├── 请求列表页面                                         │
│     ├── 解析文章基础信息 (id, link, status)                  │
│     ├── 过滤: 只保留状态为"通过"的文章                        │
│     ├── 过滤: 排除已存在于数据库的文章 (by source_id)         │
│     ├── 将新文章插入数据库 (status=pending)                   │
│     └── 等待间隔 (避免请求过快)                               │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                   4. 逐篇爬取详情                             │
│                                                             │
│   for article in pending_articles:                          │
│     ├── 请求详情页面                                         │
│     ├── 解析详情内容                                         │
│     ├── 更新数据库 (content, author, etc.)                   │
│     ├── 标记状态为 crawled                                   │
│     ├── 如失败则标记为 failed + 记录错误                      │
│     └── 等待间隔                                             │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                   5. 更新会话状态                             │
│              记录完成时间、统计信息                            │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 状态流转

```
文章状态 (news.status):

  [新发现] ──创建──▶ pending ──爬取成功──▶ crawled
                        │
                        └──爬取失败──▶ failed
```

---

## 3. 性能优化策略

考虑到服务器配置 (2C2G ECS，还有其他服务)，采用以下策略：

### 3.1 请求控制

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 列表页请求间隔 | 2000ms | 每页请求后等待 |
| 详情页请求间隔 | 1500ms | 每篇文章请求后等待 |
| 并发数 | 1 | 串行执行，不并发 |
| 单次任务最大页数 | 50 | 防止单任务过大 |
| 批处理大小 | 10 | 每处理10篇文章后暂停检查 |

### 3.2 内存优化

- 不在内存中缓存大量数据
- 逐条处理文章，即时写入数据库
- 不存储 raw_html（可配置开启）

### 3.3 错误隔离

- 单篇文章失败不影响其他文章
- 记录详细错误信息便于排查
- 支持断点续爬（从 pending 状态继续）

---

## 4. API 设计

### 4.1 创建爬取任务

```
POST /api/crawl/start

Request:
{
  "startPage": 1,      // 起始页码 (必填)
  "endPage": 10,       // 结束页码 (必填)
  "skipExisting": true // 跳过已存在的文章 (默认 true)
}

Response:
{
  "success": true,
  "sessionId": "uuid",
  "message": "爬取任务已创建"
}
```

### 4.2 查询任务状态

```
GET /api/crawl/status?sessionId={sessionId}

Response:
{
  "sessionId": "uuid",
  "status": "running", // running | paused | completed | failed
  "progress": {
    "currentPage": 5,
    "totalPages": 10,
    "totalNews": 150,
    "crawledNews": 45,
    "failedNews": 2
  },
  "startedAt": "2025-12-25T10:00:00Z",
  "updatedAt": "2025-12-25T10:05:00Z"
}
```

### 4.3 暂停/继续任务

```
POST /api/crawl/pause
POST /api/crawl/resume

Request:
{
  "sessionId": "uuid"
}
```

### 4.4 重试失败的文章

```
POST /api/crawl/retry-failed

Request:
{
  "sessionId": "uuid" // 可选，不传则重试所有失败的
}
```

---

## 5. 数据库操作

### 5.1 插入新文章 (列表阶段)

```sql
INSERT INTO news (source_id, title, source_url, region, publish_time, status)
VALUES ($1, $2, $3, $4, $5, 'pending')
ON CONFLICT (source_id) DO NOTHING
RETURNING id;
```

### 5.2 更新文章详情

```sql
UPDATE news SET
  content = $1,
  content_text = $2,
  author = $3,
  source_name = $4,
  category = $5,
  status = 'crawled',
  crawl_time = NOW()
WHERE source_id = $6;
```

### 5.3 标记失败

```sql
UPDATE news SET
  status = 'failed',
  error_message = $1
WHERE source_id = $2;
```

### 5.4 查询待爬取文章

```sql
SELECT source_id, source_url 
FROM news 
WHERE status = 'pending'
ORDER BY created_at ASC
LIMIT 100;
```

---

## 6. 地区识别规则

根据标题关键词识别文章所属地区：

```typescript
const AREA_KEYWORDS = {
  '屯溪区': ['屯溪'],
  '黟县': ['黟县'],
  '祁门县': ['祁门'],
  '歙县': ['歙县'],
  '休宁县': ['休宁'],
  '黄山区': ['黄山区'],
  '徽州区': ['徽州区'],
};

// 默认地区
const DEFAULT_AREA = '黄山';
```

---

## 7. 用户界面

### 7.1 任务创建页面

```
┌────────────────────────────────────────────┐
│           创建爬取任务                      │
├────────────────────────────────────────────┤
│                                            │
│  起始页码: [____1____]                      │
│                                            │
│  结束页码: [____10___]                      │
│                                            │
│  ☑ 跳过已爬取的文章                         │
│                                            │
│  [开始爬取]                                 │
│                                            │
└────────────────────────────────────────────┘
```

### 7.2 任务进度页面

```
┌────────────────────────────────────────────┐
│           爬取进度                          │
├────────────────────────────────────────────┤
│                                            │
│  状态: 🟢 运行中                            │
│                                            │
│  页码进度: 5 / 10                           │
│  ████████████░░░░░░░░ 50%                  │
│                                            │
│  文章统计:                                  │
│  - 发现文章: 150 篇                         │
│  - 已爬取: 45 篇                            │
│  - 失败: 2 篇                               │
│                                            │
│  [暂停] [停止]                              │
│                                            │
└────────────────────────────────────────────┘
```

---

## 8. 配置项

```typescript
// src/lib/crawl-config.ts

export const CRAWL_CONFIG = {
  // 请求间隔 (毫秒)
  LIST_PAGE_DELAY: 2000,
  DETAIL_PAGE_DELAY: 1500,
  
  // 限制
  MAX_PAGES_PER_TASK: 50,
  BATCH_SIZE: 10,
  
  // 重试
  MAX_RETRIES: 3,
  RETRY_DELAY: 5000,
  
  // 存储
  SAVE_RAW_HTML: false,
};
```

---

## 9. 错误处理

| 错误类型 | 处理方式 |
|----------|----------|
| 登录失效 | 暂停任务，通知用户重新登录 |
| 网络超时 | 重试 3 次，仍失败则标记为 failed |
| 解析失败 | 标记为 failed，记录原始 HTML (如开启) |
| 数据库错误 | 暂停任务，记录日志 |
| 请求频率限制 | 自动增加延迟时间 |

---

## 10. 实现清单

- [ ] 列表页解析器 (`src/lib/crawl/list-parser.ts`)
- [ ] 详情页解析器 (`src/lib/crawl/detail-parser.ts`)
- [ ] 爬取任务管理器 (`src/lib/crawl/task-manager.ts`)
- [ ] Supabase 数据操作 (`src/lib/supabase.ts`)
- [ ] API 路由
  - [ ] `POST /api/crawl/start`
  - [ ] `GET /api/crawl/status`
  - [ ] `POST /api/crawl/pause`
  - [ ] `POST /api/crawl/resume`
  - [ ] `POST /api/crawl/retry-failed`
- [ ] 前端页面
  - [ ] 任务创建页面
  - [ ] 任务进度页面
  - [ ] 任务历史列表
